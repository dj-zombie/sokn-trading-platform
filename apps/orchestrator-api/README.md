# 🧭 Orchestrator API (Hub)

This is the orchestrator "hub" service that coordinates trading signals and data flow across services.

## 📦 Scripts

- `pnpm dev` — Start the service in dev mode
- `pnpm build` — Compile TypeScript
